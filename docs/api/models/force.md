# Force (Oscillating) RMA

::: rma_kinetics.models.ForceRMA
    options:
      members:
        - simulate
        - _model
        - _force_rma_prod_rate
        - _apply_noise
        - max_temporal_resolution
